<form>
    <input type="search" name="term" id="term" value="<?= htmlspecialchars($term) ?>">
    <button type="submit">Search</button>
</form>
<ul>
    <?php foreach ($users as $user): ?>
      <li><i><?= htmlspecialchars($user) ?></i></li>
    <?php endforeach ?>
</ul>